import 'package:flutter/material.dart';

class AppColors {
  // Primary and background colors
  static const Color baseColor = Color(0xFFFFD233);
  static const Color whiteColor = Colors.white;
  static const Color background = Color(0xFFF8F9FA);

  // Text colors
  static const Color textPrimary = Color(0xFF2C3E50);
  static const Color textSecondary = Color(0xFF95A5A6);

  // Additional colors for UI
  static const Color cardBackground = Colors.white;
  static const Color borderColor = Color(0xFFE0E0E0);

  // For backwards compatibility
  final Color baseColorInstance = baseColor;
  final Color whiteColorInstance = whiteColor;
}
